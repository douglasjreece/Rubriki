@if (!string.IsNullOrEmpty(Message))
{
    <div class="alert alert-danger content-width" role="alert">
        @Message
    </div>
}

@code {
    [Parameter]
    public string? Message { get; set; }
}
